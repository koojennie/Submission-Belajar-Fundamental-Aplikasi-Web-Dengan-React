# Application Name and Version
Application = "GLOBAL.CICD.ReactApp"
Application_Version = "1.0.0"

# Component info
Name = "GLOBAL.CICD.ReactWebApp"
Variant = "${GIT_BRANCH}"
Version = "v1.0.${BUILD_NUM}-g${SHORT_SHA}"

[Attributes]
    GitRepo = "https://github.com/koojennie/Submission-Belajar-Fundamental-Aplikasi-Web-Dengan-React"
    GitCommit = "${GIT_COMMIT}"
    DockerRepo = "${DOCKERREPO}"
    DockerTag = "${IMAGE_TAG}"
    ServiceOwner = "${DHUSER}"
    ServiceOwnerEmail = "togetherforever1404@gmail.com"
